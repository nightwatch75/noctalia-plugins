# Claude Code local session data (permissions, personal settings)
.claude/

# Thumbnail SVG sources (local only — rendered to <plugin>/thumbnail.webp)
assets/

# Pre-generator thumbnail backups (local only)
thumbnail-old.webp
